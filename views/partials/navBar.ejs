<!-- views/partial/navbar.ejs -->
<nav class="navbar fixed-top navbar-expand-lg navbar-light" style="background-color: #A3C2F4;">
  <a class="navbar-brand" href="/"><h4> AnswerXchange </h4> </a>

  <button class="navbar-toggler" type="button" data-toggle="collapse"
  data-target="#navbar" aria-controls="navbar"
  aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbar">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <a class="nav-link" href="/home" > <h4> Home </h4> </a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/search"> <h4> Search </h4> </a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/question_upload"> <h4> Upload </h4> </a>
      </li>
    </ul>
    <span class= "mx-2 lead">
      <% if (locals.user) { %>
        <div class="dropdown">
          <button class="btn dropbtn" type="button" class="dropdown-button"
              data-toggle="dropdown"> <h4> <%= user.username %> </h4>
          <span class="caret"></span></button>
          <ul class="dropdown-content list-unstyled">
            <li><a class="dropdown-item" href=<%= user.edit_page %>>
                  Edit profile
                </a></li>
            <li><a class="dropdown-item" href="/logout">Logout</a></li>
          </ul>
        </div>
      <% } %>
    </span>
  </div>
</nav>

<style>
/* Dropdown Button */
.dropbtn {
  color: white;
  background-color: transparent;
  padding-right: 2em;
  font-size: 20px;
  border: none;
  display: block;
}

/* The container <div> - needed to position the dropdown content */
.dropdown {
  position: relative;
  display: inline-block;
}

/* Dropdown Content (Hidden by Default) */
.dropdown-content {
  display: none;
  position: absolute;
  background-color: #ffffff;
  width: 110px;
  box-shadow: 0px 4px 4px 0px rgba(0,0,0,0.2);

  z-index: 1;
}

/* Links inside the dropdown */
.dropdown-content a {
  color: black;
  padding: 12px 10px;
  text-decoration: none;
  display: block;
}

/* Change color of dropdown links on hover */
.dropdown-content a:hover {
  color: white;
  background-color: #28549C;
}

/* Show the dropdown menu on hover */
.dropdown:hover .dropdown-content {display: block;}

/* Change the background color of the dropdown button when the dropdown content is shown */
.dropdown:hover .dropbtn {background-color: transparent;}
</style>
